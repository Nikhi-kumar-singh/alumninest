<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Events Page</title>
  <!-- <link rel="stylesheet" href="/styles.css"> Add your styles if needed -->
</head>
<body>
  <h1>Upcoming Events</h1>

  <div class="events-container">
    <% if (events.length > 0) { %>
      <% for (let event of events) { %> <!-- Use 'for...of' equivalent in EJS -->
        <div class="event-card">
          <h2><%= event.name %></h2>
          <p><strong>Description:</strong> <%= event.description %></p>
          <p><strong>Type:</strong> <%= event.type %></p>
          <p><strong>Start Date:</strong> <%= event.date.start.toDateString() %></p>
          <p><strong>End Date:</strong> <%= event.date.end.toDateString() %></p>
          <p><strong>Location:</strong> <%= event.location %></p>
          <p><strong>Organiser:</strong> <%= event.organiser_name %> (<%= event.organiser_type %>)</p>
          <p><strong>Capacity:</strong> <%= event.capacity %></p>
          <p><strong>Price:</strong> $<%= event.price %></p>
          <p><strong>Registration Deadline:</strong> <%= event.registration_deadline.toDateString() %></p>
          <img src="<%= event.image %>" alt="Event Image" style="width: 200px; height: auto;">
        </div>
      <% } %>
    <% } else { %>
      <p>No events available.</p>
    <% } %>
  </div>
</body>
</html>
